# Seed invoices (OT-Main)

Satu file per Perfex invoice. Daftarkan di `manifest.php` → `invoices`.

**Batch:**
- **2025** Projectservice: `ps_inv_100` … `ps_inv_110`
- **2025** CemFlexX/TP: `cx_inv_111` … `tp_inv_126` (**INV-122 discarded — not seeded**)
- **2026**: INV-101 … 116 (T1 + commercial where both exist; INV-115 = REVISED only)

Nomor `number` boleh sama antar tahun — display format `YYYY-INV-NNN` dari tanggal invoice.

Lihat: `customize/docs/SEED-DEMO-DATA.md`, `customize/docs/SEED-FROM-PDF.md`.
